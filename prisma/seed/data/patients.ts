/**
 * Patient / intake demo fixtures will live here (exported const + builders).
 * Empty until `Patient` model gains demographics + intake status columns.
 */
export const demoPatientSeeds = [] as const;
