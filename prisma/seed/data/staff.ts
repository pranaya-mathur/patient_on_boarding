/** Demo staff fixtures — safe to commit; no secrets. */
export const demoStaffUsers = [
  { email: "intake.demo@clinic.example", name: "Jordan Intake" },
  { email: "eligibility.demo@clinic.example", name: "Riley Eligibility" },
] as const;
