import type { ReactNode } from "react";

/** Intake UI is self-contained in `IntakeSessionClient` (header, progress, sticky actions). */
export default function IntakeTokenLayout({ children }: { children: ReactNode }) {
  return children;
}
