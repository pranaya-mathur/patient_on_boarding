# `components/ui/`

Reserved for **shadcn/ui** primitives (`button`, `input`, `dialog`, …).  
Run `npx shadcn@latest init` when you are ready to add the registry; keep custom composites in `patient/`, `staff/`, or `shared/`.
